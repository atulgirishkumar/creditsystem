FROM amazoncorretto:21

ARG GRAVITON_ARTIFACT
ENV GRAVITON_ARTIFACT ${GRAVITON_ARTIFACT}

WORKDIR /home

COPY ${GRAVITON_ARTIFACT} /home/

ENTRYPOINT ["sh", "-c", "java -jar /home/${GRAVITON_ARTIFACT} $0 $1 $2 $3"]